-- Displays rows ordered by score in descending order

-- DIsplay all rows where score >= 10: Records should be ordered by score (top first)
SELECT * FROM second_table WHERE score >= 10 ORDER BY score DESC;
